export default function Header() {
	return (
		<header>
			<h1>CSS Aspect Ratio Calculator</h1>
			<span>
				A simple aspect ratio calculator that takes a media element's width and
				height and creates a css-ready output using the greatest common divisor.
			</span>
		</header>
	);
}
